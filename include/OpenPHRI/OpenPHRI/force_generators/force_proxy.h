/*      File: force_proxy.h
 *       This file is part of the program open-phri
 *       Program description : OpenPHRI: a generic framework to easily and
 * safely control robots in interactions with humans Copyright (C) 2017 -
 * Benjamin Navarro (LIRMM). All Right reserved.
 *
 *       This software is free software: you can redistribute it and/or modify
 *       it under the terms of the LGPL license as published by
 *       the Free Software Foundation, either version 3
 *       of the License, or (at your option) any later version.
 *       This software is distributed in the hope that it will be useful,
 *       but WITHOUT ANY WARRANTY without even the implied warranty of
 *       MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE. See the
 *       LGPL License for more details.
 *
 *       You should have received a copy of the GNU Lesser General Public
 * License version 3 and the General Public License version 3 along with this
 * program. If not, see <http://www.gnu.org/licenses/>.
 */

//! \file force_proxy.h
//! \author Benjamin Navarro
//! \brief Generates a force based on an externally managed one.
//! \date 05-2019
//! \ingroup phri

#pragma once

#include <OpenPHRI/force_generators/force_generator.h>
#include <OpenPHRI/definitions.h>
#include <OpenPHRI/detail/universal_wrapper.hpp>

#include <physical_quantities/spatial/force.h>

namespace phri {

//! \brief Generates a force based on an externally managed one.
//! \details Can be useful to add a force generated by an external library or
//! a force sensor.
class ForceProxy : public ForceGenerator {
public:
    using generator = std::function<spatial::Force(void)>;

    //! \brief Construct a new ForceProxy object with an initial force set
    //! to zero.
    //! \details Use ForceProxy::force() to set it to the desired value
    // ForceProxy(spatial::Frame frame);
    ForceProxy();

    //! \brief Construct a new ForceProxy object using the given
    //! spatial::Force value, reference or (shared) pointer
    //!
    //! If force is a const reference/pointer, using force() to modify it will
    //! result in undefined behavior
    //!
    //! \tparam ExtForceT The type of the value (automatically deduced)
    //! \param value The desired force (N, Nm)
    template <typename ExtForceT,
              typename std::enable_if_t<
                  not std::is_same_v<ExtForceT, generator>, int> = 0>
    explicit ForceProxy(ExtForceT&& force) noexcept
        : external_force_{std::forward<ExtForceT>(force)} {
    }

    //! \brief Construct a new ForceProxy object forwarding the force
    //! generated by \p generator
    //! \param generator The function to call to get the force to forward
    ForceProxy(const generator& generator);

    //! \brief Read/write access the force used by the generator
    //! \return double& A reference to the force
    void setForce(const spatial::Force& force);

    //! \brief Read access the force used by the generator
    //! \return double The force value
    [[nodiscard]] const spatial::Force& getForce() const;

protected:
    void update(spatial::Force& force) override;

    detail::UniversalWrapper<spatial::Force> external_force_;
    generator generator_;
};

} // namespace phri
